---
title: TCP/IP Stream
description: Stream frames from a TCP/IP port of a Senxor device and display them in a window using OpenCV.
navigation:
  title: TCP/IP Stream
  order: 5
---

# TCP/IP Stream

This example shows how to stream frames from a TCP/IP port of a Senxor device and display them in a window using OpenCV.

```python
--8<-- "example/tcpip_stream.py"
```
